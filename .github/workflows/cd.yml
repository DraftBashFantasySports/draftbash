name: Deploy Docker Container to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Docker
      run: |
        sudo apt-get update
        sudo apt-get install apt-transport-https ca-certificates curl software-properties-common
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
        sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
        sudo apt-get update
        sudo apt-get install docker-ce

    - name: Login to DockerHub
      run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

    - name: Pull Docker image
      run: docker pull sfeddes/draftbash:users-api

    - name: Install SSH key
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

    - name: Run Docker container on EC2 instance
      run: |
        echo "$EC2_SSH_KEY" > key.pem
        chmod 600 key.pem
        ssh -i key.pem ubuntu@ec2-3-144-169-40.us-east-2.compute.amazonaws.com 'docker run -p 80:3000 sfeddes/draftbash:users-api'
        env:
            EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}